services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./clips:/app/clips
      - ./logs:/app/logs
      - ./output:/app/output
    restart: unless-stopped
